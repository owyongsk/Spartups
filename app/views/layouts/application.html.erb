<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Spartups</title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <!-- Le styles -->
    <%= stylesheet_link_tag "application", :media => "all" %>

  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="brand" href="/">Spartups</a>
          <ul class="nav">
            <li><a href="/about">About</a></li>
            <li><a href="/profiles">Profiles</a></li>
          </ul>
          <div class="container nav-collapse">
            <% if user_signed_in?  %>
              <p class="navbar-text pull-right">
                Signed in as <%= link_to current_user.email, 
                                         edit_profile_path(current_user.profile) %>
                <%= link_to "Sign out", signout_path %>
              </p>
            <% else %>
              <p class="navbar-text pull-right">
                <%= link_to "Sign up", signup_path %> or
                <%= link_to "Sign in", signin_path %>
              </p>
            <% end %>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
        <div class="content">
          <div class="row">
            <% if false %>
              <% if user_signed_in? %>
                <div class="span8">
              <% else %>
                <div class="span8 offset2">
                  <%= render 'layouts/info' %>
              <% end %>
            <% end %>

            <div class="span12">
              <% [:notice, :error, :alert].each do |level| %>
              <% unless flash[level].blank? %>
                <div data-alert="alert" class="alert alert-<%= flash_class(level) %> fade in">
                  <a class="close" data-dismiss="alert" href="#">&times;</a>
                  <%= content_tag :p, flash[level] %>
                </div>
                <% end %>
              <% end %>
              <%= yield %>
            </div>
          </div>
        </div>

      <footer>
      &#169; Spartups
      <a href="https://www.facebook.com/groups/341892389233348/">
        <%= image_tag asset_path('Facebook.png') %>
      </a>
      <a href="https://twitter.com/spartups">
        <%= image_tag asset_path('Twitter.png') %>
      </a>
      <a href="http://www.linkedin.com/groups/Spartups-4627103">
        <%= image_tag asset_path('LinkedIn.png') %>
      </a>
      </footer>

    </div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
    <%= javascript_include_tag "http://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/2.0.1/bootstrap.min.js" %>

  </body>
</html>
